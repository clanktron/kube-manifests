apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: k8s-tls
  namespace: default
spec:
  secretName: k8s-tls
  privateKey:
    rotationPolicy: Always 
  dnsNames:
    - k8s.claytonc.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io
