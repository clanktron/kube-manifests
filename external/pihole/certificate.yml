apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pihole-tls
  namespace: external
spec:
  secretName: pihole-tls
  privateKey:
    rotationPolicy: Always 
  dnsNames:
    - pihole.claytonc.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io
