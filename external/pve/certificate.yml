apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pve-tls
  namespace: external
spec:
  secretName: pve-tls
  privateKey:
    rotationPolicy: Always 
  dnsNames:
    - pve.claytonc.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io
