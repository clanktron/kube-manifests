apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: flame-tls
  namespace: flame
spec:
  secretName: flame-tls
  privateKey:
    rotationPolicy: Always 
  dnsNames:
    - dash.claytonc.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io
