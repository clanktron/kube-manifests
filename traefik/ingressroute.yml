---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: traefik
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`traefik.claytonc.dev`)
      kind: Rule
      middlewares:
        - name: local-whitelist
      services:
        - name: api@internal
          kind: TraefikService
#      middlewares:
 #       - name: auth
  # Use the secret generated by cert-manager
  tls:
    secretName: traefik.claytonc.dev
