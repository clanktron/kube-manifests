apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: uptime-tls
  namespace: uptime
spec:
  secretName: uptime-tls
  privateKey:
    rotationPolicy: Always 
  dnsNames:
    - uptime.claytonc.dev
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io
